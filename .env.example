# Network Configuration
# Options: testnet (default) or mainnet
ZKVERIFY_NETWORK=testnet

# Seed Phrases (required - use different wallets for testnet and mainnet!)
ZKVERIFY_TESTNET_SEED_PHRASE="your testnet seed phrase"
ZKVERIFY_MAINNET_SEED_PHRASE="your mainnet seed phrase"

# Registered Verification Keys (run `npm run register:vk` to obtain)
# Required for server to accept proof submissions
REGISTERED_VK_HASH_TESTNET="0x..."  # Statement hash from VK registration on testnet
REGISTERED_VK_HASH_MAINNET="0x..."   # Statement hash from VK registration on mainnet

# Transaction Queue Configuration
# These settings control how proof submissions are managed to prevent nonce conflicts
QUEUE_MAX_CONCURRENT=1           # Maximum number of concurrent transactions (default: 1)
QUEUE_RETRY_ATTEMPTS=3           # Maximum retry attempts for failed transactions (default: 3)
QUEUE_RETRY_DELAY=5000            # Delay between retry attempts in milliseconds (default: 5000)
QUEUE_TIMEOUT=300000             # Timeout for stuck transactions in milliseconds (default: 300000 = 5min)

# Optional: Custom RPC endpoints (recommended for production with Ankr)
# Get free API key: https://www.ankr.com/web3-api/chains-list/zkverify/
# Mainnet example:
# ZKVERIFY_RPC_WSS=wss://rpc.ankr.com/zkverify_mainnet/ws/YOUR_API_KEY
# ZKVERIFY_RPC_HTTPS=https://rpc.ankr.com/zkverify_mainnet/YOUR_API_KEY
# Testnet example:
# ZKVERIFY_RPC_WSS=wss://rpc.ankr.com/zkverify_volta_testnet/ws/YOUR_API_KEY
# ZKVERIFY_RPC_HTTPS=https://rpc.ankr.com/zkverify_volta_testnet/YOUR_API_KEY
